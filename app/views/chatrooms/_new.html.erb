<%= simple_form_for [@offer, @chatroom] do |f| %>
  <%= hidden_field_tag(:name, @offer.title)%>
  <%# <%= hidden_field_tag(:user_id, current_user.id) %>
  <%= button_tag do %>
   <i class="fa-solid fa-envelope fa-lg"></i>
  <% end %>
  <%# <%= f.button :submit, "Contact" %>
<% end %>
